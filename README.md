# Yan and Yang

A single page wedding invitation built with Vite, React, and TypeScript.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The generated `dist` directory can be deployed directly to Vercel. Vercel also detects the Vite configuration automatically when the repository is imported.
